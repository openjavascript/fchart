package org.xas.jchart.common.data.mixchart.MixChartModelItem
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	import org.xas.core.utils.Log;
	import org.xas.core.utils.StringUtils;
	import org.xas.jchart.common.BaseAttrConfig;
	import org.xas.jchart.common.Common;
	
	public class StackMixChartModelItem extends BaseMixChartModelItem
	{
		
		public function StackMixChartModelItem( _index:int, _params:Object, _config:Config )
		{
			super( _index, _params, _config );
		}
		
	}
}